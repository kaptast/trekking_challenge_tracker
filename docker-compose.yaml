services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: rootpassword
      POSTGRES_DB: trekking_challenge_tracker
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres-data: